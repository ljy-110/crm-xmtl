<!--index.wxml-->
<view class="contion">
  <view class="card" wx:for="{{ taskList }}" wx:key="task_id" bindtap="getTask" data-taskid="{{item.task_id}}">
    <view >
      <image style="width:100%;height:170px;border-radius: 5px 5px 0 0;" src="../../img/1.png"></image>
    </view>
    <!-- <view class="fiex">{{item.task_name}}</view> -->
    <view class="title">
      <text class="text-title">{{item.task_content}}</text>
    </view>
  </view>
</view>
